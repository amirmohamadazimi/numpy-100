[project]
name = "numpy-100"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Amir Azimi", email = "amirazimimhm@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "mdutils>=1.8.1",
    "nbformat>=5.11.1",
    "numpy>=2.5.3",
]

[project.scripts]
numpy-100 = "numpy_100:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
